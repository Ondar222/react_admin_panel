import { FC } from "react"
import { IAppCard } from ".."

const AppCard: FC<IAppCard<Object>> = () => {
    // const cardKeys = Object.keys(data)
    // const cardValues = Object.values(data)

    // const [keys, setKeys] = useState<Map<string, any>>(new Map(Object.entries(data)))
    // const [selectedKeys, setSelectedKeys] = useState<string[]>()

    return (
        <div className="app-card">
        </div>
    )
}

export default AppCard