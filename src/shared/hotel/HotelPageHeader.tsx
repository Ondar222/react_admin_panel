import {
    Typography,
  } from "antd";
import { FC } from "react";

const HotelPageHeader: FC = () => {
    return <Typography.Title level={2}>Мой отель</Typography.Title>;
  };

export {HotelPageHeader}